{
  "name": "typing-machine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Детский тренажёр слепой десятипальцевой печати",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check data/levels.js && node --check public/app.js && node --check public/keyboard-layouts.js && node --check public/progress.js && node scripts/check-levels.js && node scripts/check-client-logic.js"
  },
  "engines": {
    "node": ">=18"
  }
}
